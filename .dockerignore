# Keep the Docker build context small — only the WAR is needed
nbproject/
src/
test/
build/
web/

# IDE / OS artefacts
.idea/
*.iml
*.DS_Store
Thumbs.db

# Explicitly un-ignore dist/ in case it was ignored by .gitignore
!dist/
!dist/Inveniqo.war